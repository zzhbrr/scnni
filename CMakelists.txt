cmake_minimum_required(VERSION 3.7)
project(SCNNI LANGUAGES CXX)

# Presettings
set(CMAKE_CXX_STANDARD 17)
if (NOT CMAKE_BUILD_TYPE AND NOT CMAKE_CONFIGURATION_TYPES)
    message(STATUS "Setting build type to `Debug` as none was specified.")
    set(CMAKE_BUILD_TYPE "Debug")
endif ()
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

# Find 3rdparty
# find_package(OpenCV REQUIRED) # ${OpenCV_LIBS}

# Add Build library & execuable
add_library(blob SHARED src/blob.cpp)
target_include_directories(blob PUBLIC
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>
)
add_library(tensor SHARED src/tensor.cpp)
target_include_directories(tensor PUBLIC
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>
)
add_library(layer SHARED src/layer.cpp)
target_include_directories(layer PUBLIC
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>
)
add_library(layer_factory SHARED src/layer_factory.cpp)
target_include_directories(layer_factory PUBLIC
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>
)
add_library(graph SHARED src/graph.cpp)
target_include_directories(graph PUBLIC
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>
)


# 单元测试
add_subdirectory(test)
